

<h1>Dashboard</h1>
<td><%= button_to "Parkings", public_parkings_path, method: :get %>
<table>
  <thead>
    <tr>
      <th>Users</th>
      <th>Email</th>
      <th>Admin</th>



      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.admin %></td>
        <td><%= link_to 'Change Status', update_status_path(user.id), class:"btn btn-success"%></td>
        <td><%= button_to "Cancel account", destroy_user_path(user.id), data: { confirm: "Are you sure?" }, method: :delete %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

